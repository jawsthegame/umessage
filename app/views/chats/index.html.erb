<p id="notice"><%= notice %></p>

<h1>Conversations</h1>

<table id="conversations">
  <tbody>
    <% @chats.each do |chat| %>
      <tr>
        <td><%= link_to(name_from_number(chat.identifiers), chat) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
